import React from 'react';

type ListType = {
  txt: string;
  url: string;
}[];


type MenuListProps = {
  isOpen: boolean;
}

const MenuList = ({ isOpen }: MenuListProps) => {
  return <div />
}

export default MenuList;
